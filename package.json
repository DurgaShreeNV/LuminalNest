{
  "name": "luminalstay",
  "version": "1.0.0",
  "description": "Travel stay booking website",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run client\" \"npm run server\"",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Durga Shree N V",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
